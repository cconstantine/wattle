%li.nav-header.mb20
  App Environments
  %ul.filter#app_envs
    - app_envs.each do |env|
      %li
        =label_tag do
          = check_box_tag "#{prefix}[app_env][]", env, filter_set.checked?(:app_env, env)
          = env
