%ul
  = render :partial => 'group', :collection => @groupings

- content_for :sidebar do
  %li.nav-header
    Wats
  - @groupings.each do |group|
    - wattles = group.wats
    %li
      = link_to group do
        .badge=wattles.count
        = wat_heading(representative_wat(wattles))
