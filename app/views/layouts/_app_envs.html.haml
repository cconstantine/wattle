%li.nav-header.mb20
  App Environments
  %ul.filter#app_envs
    - app_envs.each do |env|
      %li
        =label_tag do
          = check_box_tag "filters[app_env][]", env, filters.checked?(:app_env, env)
          = env
